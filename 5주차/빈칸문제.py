#[  ❓  ]를 삭제해서 알맞는 코드를 넣으세요

# OpenCV 라이브러리를 불러옵니다.
[  ❓  ]

# 얼굴 검출을 위한 xml 파일을 통해 Haar Cascade 분류기를 불러옵니다.
[  ❓  ]

# 웹캠을 엽니다. (0은 기본 웹캠을 의미합니다)
[  ❓  ]

# 웹캠이 열리지 않았을 경우 에러 메시지를 출력합니다.
if not cap.isOpened():
    print("웹캠을 열 수 없습니다.")
    exit()


# 무한 반복합니다.
[  ❓  ]
    # 웹캠에서 프레임(이미지)을 한 장씩 읽어옵니다.
    [  ❓  ]

    # 프레임을 제대로 읽어오지 못했다면 루프를 종료합니다.
    if not ret:
        print("프레임을 읽어올 수 없습니다.")
        break

    # 이미지를 흑백으로 변환합니다.
    [  ❓  ]

    # 흑백 이미지에서 얼굴을 검출합니다.
    [  ❓  ]

    for (x, y, w, h) in faces:
        # 원본 컬러 이미지에 사각형을 그립니다.
        [  ❓  ]

    # 결과 이미지를 'Face Detection' 창에 보여줍니다.
    [  ❓  ]

    # 'q' 키를 누르면 무한 루프를 종료합니다.
    [  ❓  ]
        break

# 사용이 끝난 모든 자원을 해제합니다.
[  ❓  ]
[  ❓  ]